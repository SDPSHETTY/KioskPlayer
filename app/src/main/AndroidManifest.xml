<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Custom permissions for secure broadcast receivers -->
    <permission
        android:name="com.esper.kioskplayer.permission.DEBUG_CONFIG"
        android:label="KioskPlayer Debug Configuration"
        android:description="@string/perm_debug_config_description"
        android:protectionLevel="signature" />

    <permission
        android:name="com.esper.kioskplayer.permission.REMOTE_CONTROL"
        android:label="KioskPlayer Remote Control"
        android:description="@string/perm_remote_control_description"
        android:protectionLevel="signature" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KioskPlayer">

        <meta-data
            android:name="android.content.APP_RESTRICTIONS"
            android:resource="@xml/restrictions" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="unspecified">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".ManagedConfigReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.APPLICATION_RESTRICTIONS_CHANGED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".DebugConfigReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="com.esper.kioskplayer.permission.DEBUG_CONFIG">
            <intent-filter>
                <action android:name="com.esper.kioskplayer.DEBUG_APPLY_CONFIG" />
                <action android:name="com.esper.kioskplayer.DEBUG_CLEAR_CONFIG" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".ControlReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="com.esper.kioskplayer.permission.REMOTE_CONTROL">
            <intent-filter>
                <action android:name="com.esper.kioskplayer.REFRESH_NOW" />
                <action android:name="com.esper.kioskplayer.HEALTH_DUMP" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
